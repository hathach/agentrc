name = "pr-review-validator"
description = "Load the canonical pr-review-validator agent from ~/.codex/agents/pr-review-validator.md."
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
developer_instructions = """
Before acting, read ~/.codex/agents/pr-review-validator.md completely and follow its body as your role instructions. Ignore its Claude-specific YAML frontmatter.
"""
